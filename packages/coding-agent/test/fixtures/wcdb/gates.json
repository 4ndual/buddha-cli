{
  "format": "omp-wcdb-gates-v1",
  "gates": [
    { "id": "differential.header", "suite": "differential", "observable": "meaningful headers match in JSONL and DB" },
    { "id": "differential.graph", "suite": "differential", "observable": "selected-leaf event graph and tree match" },
    { "id": "differential.tool-pairing", "suite": "differential", "observable": "tool call/result pairing matches" },
    { "id": "differential.context", "suite": "differential", "observable": "context messages and deterministic hash match" },
    { "id": "differential.compaction", "suite": "differential", "observable": "compaction boundary and checkpoint tail match" },
    { "id": "differential.labels-custom-attachments", "suite": "differential", "observable": "labels, custom data, provenance, and binary payloads match" },
    { "id": "differential.title-slot-history", "suite": "differential", "observable": "mutable title slot and append-only title history remain distinct" },
    { "id": "roundtrip.bundle-db-bundle-db", "suite": "roundtrip", "observable": "semantic versions, forks, aliases, metadata, and payload hashes are invariant" },
    { "id": "roundtrip.old-v1", "suite": "roundtrip", "observable": "OMP v1 migrates and survives round trip" },
    { "id": "roundtrip.old-v2", "suite": "roundtrip", "observable": "OMP v2 migrates and survives round trip" },
    { "id": "identity.absent-null", "suite": "roundtrip", "observable": "absent and null hash differently" },
    { "id": "identity.precision-binary", "suite": "roundtrip", "observable": "decimal precision and binary bytes survive canonical hashing" },
    { "id": "fork.simultaneous-appends", "suite": "fork", "observable": "CAS loser becomes a sibling without reparenting" },
    { "id": "fork.historical-edit", "suite": "fork", "observable": "historical edit creates a sibling and preserves old version" },
    { "id": "fork.title-only", "suite": "fork", "observable": "title-only edit creates a metadata version" },
    { "id": "fork.truncation", "suite": "fork", "observable": "shorter history is a preserved version, not deletion" },
    { "id": "fork.renamed-file", "suite": "fork", "observable": "renaming adds an alias without changing origin" },
    { "id": "fork.duplicate-id-across-harnesses", "suite": "fork", "observable": "same native id in different namespaces remains separate" },
    { "id": "fork.reordered-serialization", "suite": "fork", "observable": "tree serialization order creates no semantic fork" },
    { "id": "fork.lost-sidecar", "suite": "fork", "observable": "embedded provenance maps exactly or stays unresolved without guessing" },
    { "id": "fork.copied-export", "suite": "fork", "observable": "copied export is idempotent" },
    { "id": "fork.missing-parent", "suite": "fork", "observable": "missing parent is quarantined" },
    { "id": "fork.cyclic", "suite": "fork", "observable": "cycle is quarantined with bounded termination" },
    { "id": "fork.malformed", "suite": "fork", "observable": "malformed and truncated input is quarantined" },
    { "id": "fork.explicit-deletion", "suite": "fork", "observable": "absence and delete requests do not propagate deletion" },
    { "id": "fork.repeated-sync-orders", "suite": "fork", "observable": "repeat sync in both orders creates zero extra forks" },
    { "id": "fault.normalization-kill", "suite": "fault", "observable": "partial normalized files remain unpublished" },
    { "id": "fault.transaction-kill", "suite": "fault", "observable": "uncommitted transaction is absent after reopen" },
    { "id": "fault.acknowledged-commit", "suite": "fault", "observable": "commit persisted before acknowledgement recovers exactly once" },
    { "id": "fault.export-kill", "suite": "fault", "observable": "partial generation remains unpublished and receipt repairs idempotently" },
    { "id": "fault.mode-flip-kill", "suite": "fault", "observable": "previous mode and generation remain selected" },
    { "id": "fault.indexing-kill", "suite": "fault", "observable": "index watermark exposes incompleteness and rebuild restores results" },
    { "id": "fault.checkpoint-kill", "suite": "fault", "observable": "invalid checkpoint is ignored and ancestry replay matches" },
    { "id": "fault.disk-full", "suite": "fault", "observable": "durable failure is reported without publication" },
    { "id": "fault.short-write", "suite": "fault", "observable": "partial bytes are rejected and unpublished" },
    { "id": "fault.busy-lock", "suite": "fault", "observable": "bounded busy timeout surfaces without data loss" },
    { "id": "fault.corrupt-db-wal", "suite": "fault", "observable": "integrity fails closed and recovery runs on a copy" },
    { "id": "fault.native-unavailable", "suite": "fault", "observable": "DB health is disabled without JSONL fallback" },
    { "id": "fault.interrupted-upgrade", "suite": "fault", "observable": "old schema snapshot remains recoverable and incompatible DB is refused" },
    { "id": "maintenance.fts-rebuild", "suite": "maintenance", "observable": "rebuild restores code/path/Unicode/Spanish/phrase/prefix results without duplicates" },
    { "id": "maintenance.backup", "suite": "maintenance", "observable": "backup verifies every branch head and payload hash" },
    { "id": "maintenance.native-crash-reopen", "suite": "maintenance", "observable": "repository reopens after isolated native crash without losing acknowledged commits" },
    { "id": "worker.byte-backpressure", "suite": "worker", "observable": "byte-bounded queue streams oversized payloads without full JS materialization" },
    { "id": "worker.timeout-cancel-shutdown", "suite": "worker", "observable": "timeouts cancel work, reject late writes, and close native handles" },
    { "id": "filesystem.db-no-jsonl", "suite": "filesystem", "observable": "DB list/search/tree/header/append/fork/tail/payload/flush never stat, open, or glob session JSONL" },
    { "id": "startup.jsonl-without-native", "suite": "filesystem", "observable": "JSONL starts and operates with native library removed" },
    { "id": "startup.db-without-native", "suite": "filesystem", "observable": "DB mode fails capability-disabled without fallback" },
    { "id": "accounting.every-source", "suite": "accounting", "observable": "every inventoried hash maps to imported, excluded, or quarantined disposition" },
    { "id": "recovery.export-continue-reimport", "suite": "recovery", "observable": "exported branch continues and reimports as the correct extension or sibling" }
  ]
}
